{
  "name": "@workspace/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "push": "echo '⚠️ Otomatik drizzle push devre dışı bırakıldı (veri kaybı önlemi). Manuel olarak çalıştırmak istersen: npx drizzle-kit push --config ./drizzle.config.ts' && exit 0",
    "push-force": "echo '⚠️ drizzle push --force devre dışı (veri kaybı önlemi). Manuel: npx drizzle-kit push --force --config ./drizzle.config.ts' && exit 0"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "drizzle-zod": "^0.8.3",
    "pg": "^8.20.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.18.0",
    "drizzle-kit": "^0.31.9"
  }
}
