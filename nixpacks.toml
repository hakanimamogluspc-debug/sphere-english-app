providers = []

[variables]
NODE_ENV = "production"
PORT = "8080"

[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@9"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install --ignore-scripts"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm install --frozen-lockfile", "pnpm --filter @workspace/api-server build"]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"
